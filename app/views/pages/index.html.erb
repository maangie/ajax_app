<table>
  <thead>
    <tr>
      <th>タイトル</th>
    </tr>
  </thead>
  <tbody>
    <% @pages.each do |page| %>
      <tr id="<%= page.id %>">
        <td>
          <% @page = page %>
          <%= render partial: 'pages_ajax/show_form' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
